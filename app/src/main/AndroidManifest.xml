<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zephyra_kotlin"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity_order_confirmada.activity_orden_confirmada"
            android:exported="false" />
        <activity
            android:name=".ui.activity_checkout.activityCheckout"
            android:exported="false" />
        <activity
            android:name=".ui.activity_product.producto_view"
            android:exported="false" />
        <activity
            android:name=".carrusel_view"
            android:exported="false" />
        <activity
            android:name=".carrusel"
            android:exported="false"
            android:label="@string/title_activity_carrusel"
            android:theme="@style/Theme.Zephyra_kotlin" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>